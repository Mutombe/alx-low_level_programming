#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char = letters;

	for( letters = 'a', letters >= 'z', letter++)
		putchar(letter);

	putchar('\n');

	return(0);
}

#include "main.h"
#include <stddef.h>
#include "math.h"
/**
 * _sqrt_recursion - returns the natuaral square root of a number
 *
 * Return: Square root
 */
int _sqrt_recursion(int n)
{
	int square;
	square = sqrt(n);

	if (square == 0)
		return(-1);
}

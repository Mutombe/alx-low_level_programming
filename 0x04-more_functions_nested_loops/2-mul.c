#include "main.h"

/**
 * mul-Multiplies a and b.
 * @a:Is the first variable.
 * @b:Is the second variable.
 * Return: Always return 0.
 */

int mul(int a, int b)
{
	int mul;

	 mul =  a * b;

	 return (mul);
}
